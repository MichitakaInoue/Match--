アカウント登録
    画像はinput type fileは前回情報をvalueで保持できないためそのまま更新すると
    nullで更新される
    画像情報の更新は一度データがdbに存在するか確認すること

アカウント更新　
    やりたいこと
        ユーザー登録の際、名前とemailを入力した情報を、oldで保持
        リクエストがあるのか、DBにデータはあるのか、バリデーションには引っかかっていないかどうかによってvalueに表示する内容を変更させること






迷った箇所
    コントローラーで処理しているデータ(変数)をビューに渡していくことについて。
        return view でviewに返すときに一緒に変数にも名前を充てがってデータをビューにわたす。

    アカウント更新  
        更新可能にするだけなら、inputタグは１個だけでも良い